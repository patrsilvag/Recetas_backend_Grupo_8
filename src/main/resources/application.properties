#  Puerto del servidor (opcional, 8080 es el por defecto)
server.port=8081

# A01:2021 - Broken Access Control
# Forzar que la sesión solo use Cookies y no reescriba el JSESSIONID en la URL
server.servlet.session.tracking-modes=cookie
# Configurar atributo SameSite para mitigar ataques CSRF
server.servlet.session.cookie.same-site=lax

# Oracle Cloud Configuration
# CONFIGURACIÓN PARA DOCKER (LINUX)
spring.datasource.url=jdbc:oracle:thin:@trecml99m4zmwh47_high?TNS_ADMIN=${TNS_ADMIN:C:/Oci/wallets}
spring.profiles.active=dev
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Esta línea es la que causaba el error principal en el log:
spring.datasource.hikari.data-source-properties.oracle.net.wallet_location=(SOURCE=(METHOD=file)(METHOD_DATA=(DIRECTORY=${TNS_ADMIN:C:/Oci/wallets})))

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# SQL initialization
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
spring.jpa.defer-datasource-initialization=true